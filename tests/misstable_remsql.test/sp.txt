local function log_event(val)
    
    local mytbl = db:table("LOCAL_dorintdb.t2")
    local row = {}
    row.id = val
    local rc = mytbl:insert(row)
    return rc
end

local function main()
    
    log_event(1)
    log_event(2)
    return 0

end
