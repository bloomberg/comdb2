This is only a skeleton to test mixed cluster combinations, since we do not have yet a way to setup those.
It contains scripts we use to validate different scenarios for mixed clusters, so that we can run them 
once a proper cluster is setup.
