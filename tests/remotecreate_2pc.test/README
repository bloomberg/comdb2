Test 2PC distributed schema changes (CREATE/DROP TABLE) with crash recovery.
Verifies that DDL operations on partitioned tables are atomic across shards,
and that participant crash after prepare does not deadlock on restart.
