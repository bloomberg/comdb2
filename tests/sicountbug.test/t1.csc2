// accounts table schema

schema {
   longlong id                      // unique customer id
   longlong acct                    // account num
   longlong bal                     // account balance
}

keys {
       "KEY_ACCOUNT"      = id + acct
}

