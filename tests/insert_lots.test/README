Insert many rows to db from multithreaded writer, 
and make sure that they all succeeded.

In this testcas, because there are lots of rows, we will be testing 
verify ability to cancel processing when client disconnects.
