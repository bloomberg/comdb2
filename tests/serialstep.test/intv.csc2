// intv table schema

schema {
   longlong id       // unique show id
   cstring  name[30] // name of the show
   intervalym ym
   intervalds ds
}


keys {
         "KEY_ID" = id
   dup   "KEY_YM" = ym
   dup   "KEY_DS" = ds
}

