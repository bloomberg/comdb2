FROM ubuntu:xenial

RUN apt-get update && \
    apt-get install -y \
    bc \
    bison \
    build-essential \
    cmake \
    flex \
    gawk \
    gdb \
    iproute2 \
    iputils-ping \
    libevent-dev \
    liblz4-dev \
    liblz4-tool \
    libprotobuf-c-dev \
    libprotobuf-c1 \
    libreadline-dev \
    libsqlite3-0 \
    libsqlite3-dev \
    libssl-dev \
    libunwind-dev \
    libuuid1 \
    libz-dev \
    libz1 \
    libz1 \
    make \
    ncurses-dev \
    net-tools \
    netcat-openbsd \
    openssh-server \
    protobuf-c-compiler \
    psmisc \
    socat \
    strace \
    tcl \
    tcl-dev \
    time \
    tzdata \
    uuid-dev

COPY . /comdb2

RUN cd /comdb2 &&  \
    rm -rf build && \
    mkdir build && \
    cd build && \
    cmake -DWITH_TCL=1 .. && \
    make -j4 && \
    make -j4 test-tools && \
    make install &&  \
    cp /comdb2/tests/docker/client / && \
    cp /comdb2/tests/docker/server / && \
    apt-get clean

ENV PATH      $PATH:/opt/bb/bin
